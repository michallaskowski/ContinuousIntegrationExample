app_identifier "net.macoscope.ci-example.match.app-store"

for_platform :ios do
  for_lane :test do
    app_identifier "net.macoscope.ci-example.match.development"
  end

  for_lane :beta do
    app_identifier "net.macoscope.ci-example.match.ad-hoc"
  end
end

